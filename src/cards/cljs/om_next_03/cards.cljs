(ns om-next-03.cards
  (:require-macros
    [devcards.core :as dc :refer [defcard defcard-doc noframe-doc deftest dom-node]]))

(defcard {}               ; main obj
         {}               ; initial data
         {:heading true})
